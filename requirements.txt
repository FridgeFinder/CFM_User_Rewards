# Runtime dependencies (bundled with Lambda via SAM build)
boto3>=1.34.0

# Development / test dependencies (not bundled)
pytest>=8.0.0
pytest-cov>=5.0.0
moto[dynamodb]>=5.0.0
ruff>=0.4.0
black>=24.0.0
